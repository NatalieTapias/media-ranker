<h2> List of Works </h2>

<section class="media-table">
  <h4>Albums</h4>
  <%= render partial: "table", locals: { works: @albums} %>
  
</section>

<section class="media-table">
  <h4>Books</h4>
    <%= render partial: "table", locals: { works: @books} %>
</section>

<section class="media-table">
  <h4>Movies</h4>
    <%= render partial: "table", locals: { works: @movies} %>
</section>


<%= link_to "Add a New Work", new_work_path%>
<%= link_to "View Top Media", root_path %>